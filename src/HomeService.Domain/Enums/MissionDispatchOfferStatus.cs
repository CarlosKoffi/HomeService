namespace HomeService.Domain.Enums;

public enum MissionDispatchOfferStatus
{
    Sent = 0,
    Accepted = 1,
    Expired = 2,
    Lost = 3,
    Cancelled = 4,
    AssignmentTimedOut = 5,
    Refused = 6
}
